rules_version = '2';

// Test mode rules - allow all reads and writes
// TODO: Implement proper security rules for production
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if request.time < timestamp.date(2026, 2, 15);
    }
  }
}
