// Global TypeScript declarations
declare global {
  // Window object for browser context
  interface Window {
    // Add any window extensions here if needed
  }

  // Declare types that might be missing
  // This helps with Firebase and other libraries
}

// Ensure this file is treated as a module
export {};